apiVersion: batch/v1
kind: Job
metadata:
  name: detlam-designb-t2000-p49
spec:
  completions: 50
  parallelism: 7 
  template:
    spec:
      containers:
        - name: detlam-cont-designb-t2000-p49
          image: jaccusaurelius/vu-msc-thesis:workspace
          command: ["bash", "scripts/determine_lambda_preliminary.sh", "designB_T2000_p49"]
          resources: 
            requests:
              cpu: 1000m 
              memory: 1000Mi 
            limits:
              cpu: 1000m
              memory: 1500Mi 
          volumeMounts:
            - name: pv-out
              mountPath: /app/vu-msc-thesis/out
      restartPolicy: Never
      volumes:
        - name: pv-out
          persistentVolumeClaim:
            claimName: pvc-out
