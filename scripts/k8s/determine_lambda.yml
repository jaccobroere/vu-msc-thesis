apiVersion: batch/v1
kind: Job
metadata:
  name: detlam
spec:
  completions: 10
  parallelism: 10
  template:
    spec:
      containers:
      - name: determine-lambda-cont
        image: jaccusaurelius/vu-msc-thesis:kube
        command: ["bash"]
        args: ["scripts/determine_lambda_sim_iter.sh"]
        volumeMounts:
        - name: pv-out
          mountPath: /app/vu-msc-thesis/out
      restartPolicy: Never
      volumes:
      - name: pv-out
        persistentVolumeClaim:
          claimName: pvc-out